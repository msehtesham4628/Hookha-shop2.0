# Product Catalog

`Fumare-Hookha.xlsx` is the single source of truth for the product catalog.

The application must not read product catalogs from `src/server/db/products-*.json` or `src/server/db/scrapedProducts.json`.
